# 🧩 Лексическая структура языка

## 1. Примеры кода

```
fn main() {
    let name = "World";
    let numbers = [1, 2, 3, 4];
    let message = "Hello, " + name + "!";

    print(message);
    print("Sum of numbers:", sum(numbers));
}
```

## 2. Ключевые слова

| Слово       | Предназначение                                  |
| ------------ | ------------------------------------------------ |
| `fn`         | Определение функции                             |
| `let`        | Объявление переменной                           |
| `return`     | Возврат значения из функции                     |
| `if`         | Условный оператор                               |
| `else`       | Альтернативный оператор                         |
| `while`      | Цикл с условием                                 |
| `for`        | Цикл по диапазону                               |
| `in`         | Используется в цикле `for`                      |
| `break`      | Прерывание цикла                                |
| `continue`   | Переход к следующей итерации цикла              |
| `true`       | Логическое значение «истина»                    |
| `false`      | Логическое значение «ложь»                      |
| `null`       | Отсутствие значения                             |
| `print`      | Встроенная функция вывода                       |
| `import`     | Подключение внешнего модуля                     |

## 3. Идентификаторы

**Идентификаторы** — это имена переменных, функций и пользовательских типов.

**Правила:**
- Состоят из латинских букв (`a–z`, `A–Z`), цифр (`0–9`) и символа подчёркивания (`_`).
- Первый символ — обязательно буква или `_`.
- Регистр **не чувствителен** (`count` = `Count`).
- Ключевые слова нельзя использовать как идентификаторы.

**Примеры допустимых идентификаторов:**
```
name, counter, _value, sumOfNumbers
```

**Недопустимые:**
```
1value, fn, while
```

## 4. Литералы

### 4.1. Литералы чисел

Поддерживаются:
- **Целые числа** (в десятичной, шестнадцатеричной и двоичной системах)
- **Числа с плавающей точкой**

**Примеры:**
```
42
0101010
0x2A
3.14
-0.5
```

**Правила:**
- Целые числа — последовательность цифр (`0–9`).
- Шестнадцатеричные начинаются с `0x` или `0X`.
- Двоичные — последовательность цифр (`0–1`).
- Числа с плавающей точкой содержат `.` .

### 4.2. Литералы строк

**Строки** заключаются в двойные кавычки `"..."`.

**Поддерживаются escape-последовательности:**
| Последовательность | Значение          |
| ------------------- | ---------------- |
| `\n`                | новая строка     |
| `\t`                | табуляция        |


**Примеры:**
```
"Hello, World!"
"Line1\nLine2"
```

**Конкатенация строк:**  
Осуществляется оператором `+` (как в Python):
```
"Hello, " + name + "!"
```

### 4.3. Списковые литералы

**Списки (массивы)** создаются с помощью квадратных скобок `[]`, элементы разделяются запятыми:

```
numbers = [1, 2, 3, 4];
names = ["Alice", "Bob", "Charlie"];
```

Пустой список:
```
empty = [];
```

## 5. Операторы

| Оператор  | Описание                             | Пример             |
| --------- | ------------------------------------ | ------------------ |
| `=`       | Присваивание                         | `x = 5`            |
| `+` `-` `*` `/` `%` | Арифметические операции    | `a + b`            |
| `++` `--` | Инкремент / декремент                | `i++`, `i--`       |
| `==` `!=` | Сравнение                            | `x == y`           |
| `<` `>` `<=` `>=` | Сравнение чисел              | `x < 10`           |
| `&&` `прямые ковычки` `!` | Логические операци   | `x && y`, `!flag`  |
| `+` (строки) | Конкатенация                      | `"a" + "b"`        |
| `[]`      | Индексация списка                    | `list[0]`          |

## 6. Прочие лексемы

| Лексема | Назначение                    |
| -------- | ----------------------------- |
| `{` `}`  | Границы блока кода            |
| `(` `)`  | Скобки для выражений и вызовов функций |
| `[` `]`  | Списковые литералы, индексация |
| `,`      | Разделитель элементов          |
| `;`      | Завершение инструкции          |
| `:`      | Используется в цикле `for x in y:` |

## 7. Комментарии

Поддерживаются **однострочные** и **многострочные** комментарии.

| Тип | Синтаксис | Пример |
| --- | ---------- | ------- |
| Однострочный | `//` до конца строки | `// Это комментарий` |
| Многострочный | `/* ... */` | `/* комментарий */` |

**Примеры:**
```
// Однострочный комментарий
/* Многострочный
   комментарий */
```
