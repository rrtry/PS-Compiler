# Примеры программ на языке ----

## 1. SumNumbers

```
let a = stoi(input());
let b = stoi(input());
let sum = a + b;
print(sum);
```

---

## 2. CircleSquare

```
let r = input();
let area = Pi * r * r;
printf(area, 2);
```

---

## 3. GeometricMean

```
let a = stoi(input());
let b = stoi(input());
let gmean = (a * b) ** 0.5;
print(gmean);
```