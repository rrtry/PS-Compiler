# Список тестов

## Класс Lexer

### Литералы, примитивные выражения и присваивания
- ✅Разбор присваивания строкового литерала переменной: `let hello = "Hello, World!";`
- ✅Разбор присваивания строки с escape-последовательностями: `let helloEscaped = "I said, \"Hello, World!\"";`
- ✅Разбор присваивания целого числа: `let number = 42;`
- ✅Разбор присваивания отрицательного числа: `let number = -42;`
- ✅Разбор присваивания числа с плавающей точкой: `let number = 3.14;`
- ✅Разбор присваивания отрицательного числа с плавающей точкой: `let number = -3.14;`
- ✅Разбор присваивания массива с элементами: `let numbers = [1, 2, 3, 4];`
- ✅Разбор присваивания пустого массива: `let empty = [];`
- ✅Разбор присваивания шестнадцатеричного числа: `let number = 0xDEADC0DE;`
- ✅Разбор присваивания двоичного числа: `let number = 0b11011110101011011100000011011110;`

### Функции

- ✅Разбор объявления функции без параметров: `fn main() { return 42; }`
- ❌Разбор объявления функции без параметров: `fn main(a) { return a; }`

### Условные конструкции

- ✅Разбор конструкций `if-else`: `if x > 0 { print("positive"); } else { print("non-positive"); }`

### Циклы

- ✅Разбор цикла `while`: `while i < 10 { i++; }`
- ✅Разбор цикла `for` по диапазону: `for x in numbers { print(x); }`

### Логические выражения

- ✅Разбор логических операций: `let flag = true && !false || false;`

### Импорт модулей

- ✅Разбор импорта внешнего модуля: `import math;`

### Сложные арифметические выражения

- ✅Разбор сложного выражения с несколькими операциями и скобками: `let result = (a + b) * c - d / e % f ^ g;`